#!/bin/bash

# このファイルがあるフォルダに自動で移動する（ここが重要）
cd "$(dirname "$0")"

# メインのプログラムを実行
python3 main.py

# Windowsの "pause" の代わり（終わった後にすぐ閉じないようにする）
echo ""
echo "========================================="
echo "プログラムが終了しました。"
read -p "エンターキーを押すと画面を閉じます..."