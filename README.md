1. Install Python
2. pip install -r requirements.txt
3. Install ffmpeg(https://www.ffmpeg.org/download.html)
4. add ffmpeg/bin folder to environment variable "Path"
5. open "start.cmd" and enter the twitch archive url
